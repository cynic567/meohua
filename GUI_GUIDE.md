# 梅花易数占卜程序 - 桌面GUI快速指南
# Meihua Yishu Desktop GUI Quick Start Guide

## 快速开始 (Quick Start)

### 一条命令启动

```bash
python3 meihua_gui.py
```

就这么简单！程序会自动完成所有设置。

## 特点

✅ **零依赖** - 只需要Python 3.6+，不需要任何额外的GUI库  
✅ **跨平台** - Windows、Mac、Linux 都支持  
✅ **自动化** - 自动启动服务器、打开浏览器  
✅ **美观** - 现代化渐变色界面设计  
✅ **完整** - 包含所有18种占卜类别  

## 使用流程

1. **启动程序**
   ```bash
   python3 meihua_gui.py
   ```

2. **程序自动执行**
   - ✓ 启动本地HTTP服务器（端口8888）
   - ✓ 创建临时HTML界面文件
   - ✓ 在浏览器中打开应用

3. **开始占卜**
   - 选择占卜类别（18种可选）
   - 选择起卦方式（时间+IP 或 仅时间）
   - 点击"开始占卜"按钮

4. **查看结果**
   - 本卦信息（上卦、下卦、卦名、动爻）
   - 变卦信息
   - 体用分析（五行关系）
   - 卦辞解释

5. **退出程序**
   - 在终端按 `Ctrl+C`
   - 程序会自动清理资源

## 界面说明

### 占卜类别区域
显示18种传统占卜类别：
- 天时占、人事占、家宅占、屋舍占
- 婚姻占、生产占、饮食占、求谋占
- 求名占、求财占、交易占、出行占
- 行人占、谒见占、失物占、疾病占
- 官讼占、坟墓占

### 起卦方式
两种选择：
- **时间+IP地址**（推荐）：使用当前时间和本机IP地址计算
- **仅使用时间**：只使用当前时间计算

### 结果显示区
格式化显示占卜结果：
- 标题和分隔线
- 占卜时间和类别
- 本卦详情（带八卦符号）
- 变卦详情
- 体用分析（五行关系）
- 卦辞解释（根据类别定制）

## 技术原理

### 为什么不需要GUI库？

传统的桌面程序需要：
- tkinter（可能缺失）
- PyQt5/PySide（需要额外安装）
- wxPython（体积大）

本程序使用：
- Python标准库的 `http.server`（内置）
- HTML + CSS + JavaScript（界面）
- 浏览器作为渲染引擎（系统自带）

### 工作原理

```
启动程序
   ↓
创建HTTP服务器（监听localhost:8888）
   ↓
生成HTML界面文件
   ↓
启动浏览器（应用模式，无地址栏）
   ↓
用户操作 → HTTP请求 → Python处理 → 返回JSON
   ↓
显示结果
   ↓
Ctrl+C 退出
```

### 端口处理

- 默认使用端口 8888
- 如果被占用，自动尝试下一个端口（8889, 8890...）
- 终端会显示实际使用的端口

## 常见问题

**Q: 为什么浏览器打开后有地址栏？**  
A: 程序会尝试在应用模式打开浏览器（无地址栏），但某些浏览器或系统可能不支持。不影响使用。

**Q: 可以关闭浏览器吗？**  
A: 可以，但要重新打开需要访问终端显示的地址。

**Q: 多次运行会怎样？**  
A: 程序会自动检测端口占用，使用下一个可用端口。

**Q: 如何在后台运行？**  
A: 
```bash
nohup python3 meihua_gui.py > /dev/null 2>&1 &
```
但建议在前台运行，方便查看状态和退出。

**Q: 数据保存在哪里？**  
A: 占卜结果只显示在界面上，不会保存。如需保存，请手动复制或截图。

**Q: 安全吗？**  
A: 是的，服务器只监听 localhost（127.0.0.1），外部无法访问。

**Q: 占卜结果准确吗？**  
A: 程序严格按照梅花易数传统理论实现，结果仅供参考娱乐。

## 自定义

### 修改默认端口

编辑 `meihua_gui.py`，找到：
```python
app = MeihuaGUI(port=8888)
```
改为：
```python
app = MeihuaGUI(port=你想要的端口)
```

### 修改界面样式

程序会生成 `gui_interface.html` 文件，可以直接编辑其中的CSS样式。

## 对比其他版本

| 特性 | 命令行版本 | Web版本 | 桌面GUI版本 |
|------|-----------|---------|------------|
| 依赖 | 无 | Flask | 无 |
| 界面 | 文本 | 浏览器 | 类桌面应用 |
| 安装 | 无需 | pip安装 | 无需 |
| 启动 | 手动运行 | 手动启动+访问 | 一键启动 |
| 体验 | 基础 | 优秀 | 最佳 |

## 贡献

欢迎提交问题和改进建议！

## 免责声明

本程序仅供娱乐和学习传统文化之用，占卜结果仅供参考。
